import React from 'react';
import SidebarNav from '../Dashboard/SidebarNav';

/**
 * Sidebar component for the main application layout.
 * It renders the primary navigation panel.
 */
const Sidebar: React.FC = () => {
  return <SidebarNav />;
};

export default Sidebar;
